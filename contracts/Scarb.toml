[package]
name = "vow_collection_probe"
version = "0.1.0"
edition = "2024_07"
cairo-version = "=2.17.0"
scarb-version = "=2.17.0"

[dependencies]
starknet = "=2.17.0"

[dev-dependencies]
snforge_std = "=0.63.0"

[[target.starknet-contract]]
sierra = true
casm = true

[cairo]
sierra-replace-ids = false

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]

[scripts]
test = "snforge test"
